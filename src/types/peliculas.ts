export type Peliculas = {
    id: string,
    nombre: string,
    descripcion: string,
    valoracion: number,
    archivoVideo: number
}

export type PeliculaEspecifica = {
    id: string,
    nombre: string,
    descripcion: string,
    valoracion: number,
    archivoVideo: number
}

export type NuevaPelicula = {
    nombre: string,
    descripcion: string,
    valoracion: number,
    archivoVideo: number
}