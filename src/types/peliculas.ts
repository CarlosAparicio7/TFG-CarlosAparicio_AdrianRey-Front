export type Peliculas = {
    id: number,
    nombre: string,
    descripcion: string,
    valoracion: number,
    archivoVideo: number
}

export type PeliculaEspecifica = {
    id: number,
    nombre: string,
    descripcion: string,
    valoracion: number,
    archivoVideo: number
}

export type NuevaPelicula = {
    nombre: string,
    descripcion: string,
    valoracion: number,
    archivoVideo: number
}